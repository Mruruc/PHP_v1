<?php
    /**
     * There are three main numeric types in PHP:
     * Integer
     * Float
     * Number String
     * 
     * in addition, PHP has more data types used for numbers.
     * Infinity
     * Nan 
     */

     // ! Variables of numeric types are created when we assign a value to them.
     
     $a=5;
     $b=5.006;
     $c="172817";

     var_dump($a);
     var_dump($b);
     var_dump($c);

     echo "<b/>";
     


?>